---
description: Verify the web_search (SearXNG) tool works and announce it to the user
---
Before doing anything else, check whether you have a `web_search` tool available.

If you do:
1. Call `web_search` once, for real, with a simple verifiable query (e.g. current date, or a current-events topic).
2. In 2-3 sentences, confirm to the user that web search works, name the tool, and give one example of when you'd use it unprompted (current events, library/API docs released after your training cutoff, prices, etc).

If you do NOT have a `web_search` tool available, say so in one sentence and stop.

Do not ask for permission first - just try it and report the outcome.
